/**
 * Codebase Auditor — public library surface.
 *
 * The CLI (src/cli/main.ts) is the primary entrypoint, but the engine is
 * exposed here so it can be embedded (future web UI / GitHub App / CI gate).
 */

export const VERSION = "0.1.0";
