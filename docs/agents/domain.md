# Domain docs layout

**Single-context.** One `CONTEXT.md` at the repo root holds the ubiquitous
language. Architectural decisions, when recorded, go under `docs/adr/`.

Consumers (`improve-codebase-architecture`, `diagnosing-bugs`, `tdd`) should read
`CONTEXT.md` for domain vocabulary before proposing changes.
